export const MAX_CBI_LEVELS_IN_CBI = 3;

export const ADD_NEW_CBI_LEVEL_FAIL_BECAUSE_HAS_USER_SUBMIT =
  'Đã có người dùng đánh giá, không thể thêm giai đoạn!';

export const UPDATE_CBI_LEVEL_FAIL_BECAUSE_HAS_USER_SUBMIT =
  'Đã có người dùng đánh giá, không thể chỉnh sửa giai đoạn!';

export const PUBLISH_CBI_LEVEL_FAIL_BECAUSE_HAS_NOT_QUESTION_REQUIRED =
  'Level cbi cần có ít nhất một câu hỏi bắt buộc trả lời';

export const RE_ORDER_CBI_LEVEL_FAIL_BECAUSE_HAS_USER_SUBMIT =
  'Đã có người dùng đánh giá, không thể sắp xếp thứ tự giai đoạn!';
